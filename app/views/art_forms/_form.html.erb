<%= form_with(model: art_form, local: true) do |form| %>
  <% if art_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(art_form.errors.count, "error") %> prohibited this art_form from being saved:</h2>

      <ul>
      <% art_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :art_form_name %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :art_form_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
