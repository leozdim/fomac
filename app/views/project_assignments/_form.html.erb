<%= form_with(model: project_assignment, local: true) do |form| %>
  <% if project_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project_assignment.errors.count, "error") %> prohibited this project_assignment from being saved:</h2>

      <ul>
      <% project_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field" hidden>
    <%= form.label :project_id %>
    <%= form.text_field :project_id, id: :project_assignment_project_id %>
  </div>

  <div class="field" hidden>
    <%= form.label :user_id %>
  </div>

  <div class="field" >
    <%= form.select :user_id, options_for_select(@judges.map{ |c| [c.full_name_space, c.id] }, selected_key = form.object.user_id) %>

  </div>


  <div class="actions">
    <%= form.submit 'Asignar' , :class => 'waves-effect waves-light btn', :style=>'color:white'%>
  </div>
<% end %>
