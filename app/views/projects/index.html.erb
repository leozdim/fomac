<p id="notice"><%= notice %></p>
<span class="card-title">Proyectos registrados</span>
<%= link_to("Proyectos evaluados",static_pages_project_scores_path, class: 'btn waves-effect waves-light blue', :style=>"color:white!important") if user_signed_in?  %>

<table id="projects-table" class="responsive-table" data-source="<%= projects_path(format: :json) %>">
  <thead>
    <tr>
      <th>ID</th>
      <th>Folio</th>
      <th>Creador</th>
      <th>Categoría</th>
      <th>Nombre</th>
      <th>Acciones</th>
      <th>Estatus</th>
    </tr>
  </thead>
  <tbody>
  </tbody>
</table>
<!--
<table>
  <thead>
    <tr>
      <th>Folio</th>
      <th>User</th>
      <th>Category</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @projects.each do |project| %>
      <tr>
        <td><%#= project.folio %></td>
        <td><%#= project.user %></td>
        <td><%#= project.category %></td>
        <td><%#= project.status %></td>
        <td><%#= link_to 'Show', project %></td>
        <td><%#= link_to 'Edit', edit_project_path(project) %></td>
        <td><%#= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <#% end %>
  </tbody>
</table>
-->
<br>

