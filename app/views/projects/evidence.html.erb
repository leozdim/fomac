
<%= form_for(@project, url: project_evidence_path ) do |f| %>
  <%= f.fields_for :visual_evidence  do |p| %> 
    <%= render 'visual_evidence_fields', f: p %>
  <% end %>
  <%= f.submit %>
<% end %>
